// postcss.config.js
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
// This configuration file is used to set up PostCSS with Tailwind CSS and Autoprefixer.
// It ensures that Tailwind CSS styles are processed correctly and that vendor prefixes are added to CSS rules as needed.